<?xml version="1.0" encoding="utf-8"?>
<log4net>

  <!-- console 輸出 -->
  <appender name="consoleAppender" type="log4net.Appender.ConsoleAppender">
    <layout type="log4net.Layout.PatternLayout">
      <conversionPattern value="%date{yyyy/MM/dd HH:mm:ss.fff} %-5level %logger - %message%newline" />
    </layout>
  </appender>

  <!-- 原本的 fileAppender：寫入 log/LOG_ 檔案（保留） -->
  <appender name="fileAppender" type="log4net.Appender.RollingFileAppender">
    <file value="log/LOG_" />
    <appendToFile value="true" />
    <rollingStyle value="Date" />
    <datePattern value="_ddHH'.txt'" />
    <layout type="log4net.Layout.PatternLayout">
      <conversionPattern value="%date{yyyy/MM/dd HH:mm:ss.fff} %-5level %logger - %message%newline" />
    </layout>
  </appender>

  <!-- Info 等級寫入 log/info/yyyyMMdd.log -->
  <appender name="InfoFileAppender" type="log4net.Appender.RollingFileAppender">
    <file value="log/info/" />
    <appendToFile value="true" />
    <rollingStyle value="Date" />
    <datePattern value="yyyyMMdd'.log'" />
    <staticLogFileName value="false" />
    <layout type="log4net.Layout.PatternLayout">
      <conversionPattern value="%date{yyyy/MM/dd HH:mm:ss.fff} %-5level %logger - %message%newline" />
    </layout>
    <filter type="log4net.Filter.LevelMatchFilter">
      <levelToMatch value="INFO" />
    </filter>
    <filter type="log4net.Filter.DenyAllFilter" />
  </appender>

  <!-- Error 等級寫入 log/error/yyyyMMdd.log -->
  <appender name="ErrorFileAppender" type="log4net.Appender.RollingFileAppender">
    <file value="log/error/" />
    <appendToFile value="true" />
    <rollingStyle value="Date" />
    <datePattern value="yyyyMMdd'.log'" />
    <staticLogFileName value="false" />
    <layout type="log4net.Layout.PatternLayout">
      <conversionPattern value="%date{yyyy/MM/dd HH:mm:ss.fff} %-5level %logger - %message%newline" />
    </layout>
    <filter type="log4net.Filter.LevelMatchFilter">
      <levelToMatch value="ERROR" />
    </filter>
    <filter type="log4net.Filter.DenyAllFilter" />
  </appender>

  <!-- 根節點：輸出到所有 Appender -->
  <root>
    <level value="ALL" />
    <appender-ref ref="consoleAppender" />
    <appender-ref ref="fileAppender" />
    <appender-ref ref="InfoFileAppender" />
    <appender-ref ref="ErrorFileAppender" />
  </root>

</log4net>
